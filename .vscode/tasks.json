{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "make gen_gdl90 DEBUG=true",
            "problemMatcher": "$go",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "PreStart",
            "type": "shell",
            "command": "killall dump1090 ogn-rx-eu || true",
            "dependsOn": [
                "Build"
            ]
        }
    ]
}